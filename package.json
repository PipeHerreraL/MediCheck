{
  "name": "medicheck-root",
  "version": "1.0.0",
  "private": true,
  "description": "Manejador raíz para MediCheck",
  "scripts": {
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\" \"npm run start:php\"",
    "start:frontend": "cd frontend && npm start",
    "start:backend": "cd backend && npm run dev",
    "start:php": "cd php_backend && php -d extension=pdo_mysql -S localhost:8000",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
